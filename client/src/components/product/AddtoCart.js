import React from 'react'
/*
export default class AddtoCart extends React.Component{
    constructor(props) {
        super(props);
    }
    return (
    this.setState(prevState => ({
        cartItems: [...prevState.cartItems, props],
        cartQty: (prevState.cartQty + 1)
      }))
    )
    
}

*/